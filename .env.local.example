NEXT_PUBLIC_API_URL=http://localhost:8080

# Set to "true" to serve static fixture data instead of hitting the real backend.
# Covers: markets list, market detail, and order books.
# Useful for frontend-only development when the backend is not running.
USE_MOCK_DATA=false
